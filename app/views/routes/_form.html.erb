<%= form_for(@route) do |f| %>
  <% if @route.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@route.errors.count, "error") %> prohibited this route from being saved:</h2>

      <ul>
      <% @route.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :destination %><br />
    <%= f.text_field :destination %>
  </div>
	<div class="field">
    <%= f.label :itn_file, "Waypoints" %><br />
    <%= f.text_area :itn_file, :rows => 3 %>
  </div>
	<div class="field">
    <%= f.hidden_field :jumps %>
  </div>
  <div class="actions">
    <%= f.submit %> <%= link_to 'Cancel', routes_path %>
  </div>
<% end %>

