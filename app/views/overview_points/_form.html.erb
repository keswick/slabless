<%= form_for(@overview_point) do |f| %>
  <% if @overview_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@overview_point.errors.count, "error") %> prohibited this overview_point from being saved:</h2>

      <ul>
      <% @overview_point.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :latlng %><br />
    <%= f.text_field :latlng %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
